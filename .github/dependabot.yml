version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      next:
        patterns: [next, react, react-dom]
      tooling:
        patterns: ["@playwright*", "@axe-core*", oxlint, vitest, vite, tailwindcss, zod]
